const getCookie=name=>{
    const cookie=document.cookie;
    const allCookie=allCookie.find(c=>c.includes(name));
    if((findCookies)){
        'country=US'
        const cookieNameValue=findCookies.split('=');
        return cookieNameValue[1];

    }
    
}